import { hooksUseMediaQuery } from './hooks-use-media-query';

describe('hooksUseMediaQuery', () => {
  it('should work', () => {
    expect(hooksUseMediaQuery()).toEqual('hooks-use-media-query');
  });
});
